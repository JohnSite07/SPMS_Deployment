# data module — single purpose: Cloud SQL (MySQL 8.4, db-f1-micro, private IP
# only) and Cloud Storage buckets (document blobs; state bucket is bootstrapped
# outside Terraform). Content added by PRD 0002.
